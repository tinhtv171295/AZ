import { ButtonAppearance } from "../UI/Button";

type TitleType = {
  title: string;
  titleBody?: string;
  linkDetail?: string;
  buttonAppearance?: ButtonAppearance;
};

export default TitleType;
